[project]
name = "packspec"
description = "PackSPEC - SPEC benchmark packaging tool"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "loguru>=0.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
